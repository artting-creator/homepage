
# 全域行為準則與系統規範 (Global Rules)

## 0. 對話語言
- **一律使用繁體中文回覆**（台灣用語）。程式碼、識別字、指令與既有英文註解維持原樣，不需翻譯。

## 1. 檔案編碼與終端機操作規範
在 Windows 環境中操作或修改檔案時，為了防止編碼損壞，請嚴格遵守以下規範：

- **優先使用原生工具**：修改原始碼時，請務必優先使用 Claude Code 內建的檔案編輯工具（Edit / Write）。這些工具原生支援 UTF-8（無 BOM）並且能安全保留所有 Unicode 字元與 Emoji。只有在原生工具無法完成的任務（例如批次處理、執行系統指令）才使用終端機工具。
- **變數展開防範**：透過 PowerShell 工具寫入包含 `$`、`${}`、反引號等字串時，**絕對不可**用雙引號內嵌變數，請改用單引號字串，或多行內容改用單引號 here-string（`@'...'@`，結尾 `'@` 必須頂格，不可縮排）。Bash 工具則避免用雙引號包住含 `$` 的內容，改用單引號跳脫。
- **Python 腳本備案**：若原生編輯與終端機工具皆無法完成複雜任務，才使用 Python 腳本，且檔案讀寫需明確指定 `encoding='utf-8'`。

## 2. 檔案格式規範 (.bat 檔)
- 若修改或生成的目標是 Windows 的 `.bat` 批次檔，必須確保儲存格式為 **UTF-8（無 BOM）**，並且行尾字元 (Line Endings) 必須是 **CRLF**，絕對不可混入 LF。

## 3. 錯誤處理與自動化中斷機制
- **高風險 / 異常失敗 (中斷並回報)**：若執行檔案修改失敗、尋找目標程式碼區塊失敗，且繼續嘗試可能會造成程式碼毀損時，請**立即停止動作並向我回報**，等待我的授權後再繼續。
- **低風險 / 已知錯誤 (自動修正)**：如果失敗原因明確且風險極低（例如：Linter 報錯、路徑拼寫錯誤），請發揮 Agent 的自主性，**自行調整修正並繼續完成任務**，最後再向我總結即可。

## 4. 程式碼撰寫與修改準則 (Karpathy Guidelines)
- **思考再行動**：修改前先閱讀並解析專案脈絡；有疑問或需求不明確時，**必須先向我提問**，絕對不可自行腦補或猜測。
- **保持極度簡單**：只寫能解決當前問題的「最少程式碼」。絕對不寫多餘的抽象層、未被要求的預測性功能、或為未來鋪路。
- **外科手術式的精準修改 (Touch only what you must)**：只修改有必要的那幾行程式碼。**絕對不允許**順手幫我重構其他不相干的程式碼、調整排版、或修改周邊的註解，以避免引發意外的錯誤。
- **目標導向與驗證**：執行複雜任務前，先確立「可驗證的成功標準」(Success Criteria)，確實驗證（例如執行測試或編譯）後再進行下一步。

